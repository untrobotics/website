apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: untrobotics-prod
resources:
  - namespace.yaml
  - ../../base
  - ingress.yaml
patches:
  - path: config-patch.yaml
# When prod uses registry images, override here (built/pushed via CI to GHCR):
# images:
#   - name: untrobotics-web
#     newName: ghcr.io/untrobotics/website
#     newTag: "1.0.0"
#   - name: untrobotics-driver-ws
#     newName: ghcr.io/untrobotics/website-driver-ws
#     newTag: "1.0.0"
