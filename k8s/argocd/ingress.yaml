# Public TLS access to the Argo CD UI. Requires a DNS A record:
#   c.untrobotics.com -> 51.81.223.48
# argocd-server runs in insecure mode (see install-argocd.sh); TLS terminates here.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server
  namespace: argocd
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - c.untrobotics.com
      secretName: c-untrobotics-tls
  rules:
    - host: c.untrobotics.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
