; Production-oriented PHP settings for the container image.
; Errors are logged (visible via `kubectl logs`) rather than printed into pages.
display_errors = Off
display_startup_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT

expose_php = Off
