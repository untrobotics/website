[mysqld]
# Local-dev MySQL tuning for the UNT Robotics site.
# mysql_native_password keeps auth simple for the PHP client over the dev network.
default-authentication-plugin = mysql_native_password
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
